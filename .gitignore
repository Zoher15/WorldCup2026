# Bare name (not just `node_modules/`) so a stray symlink named node_modules is
# ignored too, not only the real directory.
node_modules
.next/
out/
dist/
next-env.d.ts
*.tsbuildinfo
.env
.env.local
.env*.local
.DS_Store
*.log
.vercel
.claude/worktrees/
